---
title: Lokinode
description: Desktop interface for Flokicoin node operators. Manage your full node and Lightning Network operations with a native background service.
sidebar_position: 2
hide_title: true
keywords: [Lokinode, Flokicoin, GUI wallet, node manager, desktop wallet, FLND, lightning node, background service, FLN]
custom_edit_url: null
---

import Link from '@docusaurus/Link';
import { Monitor, Zap, Shield, Activity, Settings, Download, Cpu, HardDrive, Layout, ChevronRight, Terminal, Box } from 'lucide-react';
import tapHomeImage from '../tapwallet_home.png';
import Heading from '@theme/Heading';

<div className="lokinode-hero-container">
  <div className="container">
    <div className="lokinode-hero-inner">
      <div className="lokinode-hero-content">
        <Heading as="h1" className="lokinode-hero-title">Lokinode</Heading>
        <p className="lokinode-hero-subtitle">Desktop interface for managing Flokicoin full nodes and Lightning Network (FLN) operations.</p>

        <div className="hero-modern__actions lokinode-hero-actions-override">
          <Link className="hero-button hero-button--primary" href="https://github.com/flokiorg/lokinode/releases">
            <span><Download size={18} /></span><span>Download v0.1.0</span>
          </Link>
          <Link className="hero-button" href="https://github.com/flokiorg/lokinode">
            <span><Terminal size={18} /></span><span>Source Code</span>
          </Link>
        </div>

        <div className="lokinode-hero-status-card">
          <div className="status-icon-wrapper"><Activity size={22} /></div>
          <div>
            <strong>Latest Release: 0.1.0</strong>
            <p>Integrated global language support and RTL layout compatibility.</p>
          </div>
        </div>
      </div>

      <div className="lokinode-hero-visual">
        <div className="lokinode-visual-wrapper">
          <img src={tapHomeImage.src ?? tapHomeImage} alt="Lokinode Desktop GUI" className="lokinode-main-screenshot" />
          <div className="lokinode-visual-glow"></div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>{`
  .lokinode-hero-container {
    margin: 0 -2rem 4rem;
    padding: 5rem 2rem 6rem;
    background: 
      radial-gradient(ellipse at 70% 20%, rgba(208, 151, 15, 0.15) 0%, transparent 52%),
      radial-gradient(ellipse at 15% 80%, rgba(208, 151, 15, 0.08) 0%, transparent 42%),
      linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(208, 151, 15, 0.02) 100%);
    position: relative;
    overflow: hidden;
  }

  .lokinode-hero-container::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(to bottom, transparent 0%, var(--ifm-background-color) 100%);
    pointer-events: none;
    z-index: 1;
  }

  .lokinode-hero-inner {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    align-items: center;
    gap: 3.5rem;
  }

  /* Typography */
  .lokinode-hero-title {
    font-size: clamp(2.8rem, 6vw, 4.5rem);
    font-weight: 900;
    line-height: 1.0;
    margin-bottom: 1.25rem;
    letter-spacing: -0.04em;
    background: linear-gradient(135deg, #ffffff 0%, #f0b832 55%, rgba(255,255,255,0.85) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .lokinode-hero-subtitle {
    font-size: clamp(1rem, 1.8vw, 1.3rem);
    color: rgba(255, 255, 255, 0.68);
    margin-bottom: 2rem;
    max-width: 480px;
    line-height: 1.55;
  }

  .lokinode-hero-actions-override {
    margin-bottom: 2rem !important;
    margin-top: 0 !important;
  }

  /* Status Card */
  .lokinode-hero-status-card {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.1rem 1.4rem;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 18px;
    max-width: 440px;
    backdrop-filter: blur(12px);
  }

  .lokinode-hero-status-card strong {
    display: block;
    font-size: 1rem;
    font-weight: 700;
    color: #fff;
    margin-bottom: 0.2rem;
  }

  .lokinode-hero-status-card p {
    margin: 0;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.4;
  }

  .status-icon-wrapper {
    flex-shrink: 0;
    background: var(--ifm-color-primary);
    color: #000;
    padding: 10px;
    border-radius: 12px;
    display: flex;
    box-shadow: 0 6px 14px rgba(208, 151, 15, 0.35);
  }

  /* Visual wrapper */
  .lokinode-hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
    perspective: 1200px;
  }

  .lokinode-visual-wrapper {
    position: relative;
    transform: rotateY(-10deg) rotateX(2deg);
    transition: transform 0.55s cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  .lokinode-visual-wrapper:hover {
    transform: rotateY(-2deg) rotateX(1deg) scale(1.02);
  }

  .lokinode-main-screenshot {
    width: 100%;
    max-width: 480px;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 40px 80px -15px rgba(0,0,0,0.75),
                0 20px 40px -20px rgba(0,0,0,0.6);
    display: block;
  }

  .lokinode-visual-glow {
    position: absolute;
    inset: -10%;
    background: radial-gradient(circle, rgba(208, 151, 15, 0.15) 0%, transparent 70%);
    z-index: -1;
    filter: blur(40px);
    pointer-events: none;
  }

  @media (max-width: 996px) {
    .lokinode-hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 3rem;
    }
    .lokinode-hero-subtitle,
    .lokinode-hero-status-card {
      margin-left: auto;
      margin-right: auto;
    }
    .lokinode-visual-wrapper { transform: none !important; }
    .lokinode-main-screenshot { max-width: 400px; }
  }

  @media (max-width: 600px) {
    .lokinode-hero-actions-override {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .lokinode-hero-actions-override .hero-button {
      width: 100%;
      justify-content: center;
    }
  }
`}</style>

**Lokinode** is a desktop application designed for running and managing a Flokicoin full node. It provides a graphic environment for wallet operations, channel management, and real-time node monitoring.

<div className="row margin-top--lg">
  <div className="col col--4 margin-bottom--lg">
    <div className="card shadow--md padding--lg" style={{height: '100%'}}>
      <Heading as="h3"><Monitor size={20} style={{verticalAlign: 'middle', marginRight: '8px'}} /> Background Service</Heading>
      <p>Runs as a persistent background process with a system tray interface. Closing the window keeps the node active.</p>
    </div>
  </div>
  <div className="col col--4 margin-bottom--lg">
    <div className="card shadow--md padding--lg" style={{height: '100%'}}>
      <Heading as="h3"><Zap size={20} style={{verticalAlign: 'middle', marginRight: '8px'}} /> FLN Node</Heading>
      <p>Full-featured Flokicoin Lightning Network (FLN) node with integrated channel and liquidity management.</p>
    </div>
  </div>
  <div className="col col--4 margin-bottom--lg">
    <div className="card shadow--md padding--lg" style={{height: '100%'}}>
      <Heading as="h3"><Box size={20} style={{verticalAlign: 'middle', marginRight: '8px'}} /> Node Management</Heading>
      <p>Initialize and manage multiple node configurations with custom data locations and advanced security settings.</p>
    </div>
  </div>
</div>

## Download Assets

Download the latest release for your operating system from [GitHub](https://github.com/flokiorg/lokinode/releases/tag/v0.1.0).

| OS | Asset |
| --- | --- |
| **Windows** | [lokinode_0.1.0_windows_amd64.exe](https://github.com/flokiorg/lokinode/releases/download/v0.1.0/lokinode_0.1.0_windows_amd64.exe) |
| **macOS** | [lokinode_0.1.0_darwin_amd64.dmg](https://github.com/flokiorg/lokinode/releases/download/v0.1.0/lokinode_0.1.0_darwin_amd64.dmg) |
| **Linux** | [lokinode_0.1.0_linux_amd64.deb](https://github.com/flokiorg/lokinode/releases/download/v0.1.0/lokinode_0.1.0_linux_amd64.deb) |

## Installation

1.  **Download** the package for your operating system.
2.  **Run the installer**:
    *   **Windows**: Execute the `.exe` and follow the prompts.
    *   **macOS**: Open the `.dmg` and move Lokinode to Applications.
    *   **Linux**: Install via `sudo dpkg -i lokinode_*.deb`.
3.  **Setup**: Follow the on-screen instructions to create a new 24-word seed phrase or import an existing one.

## Capabilities

### Lightning Network (FLN) Operations
Lokinode provides a dedicated interface for Flokicoin Lightning Network operations:
*   **Visual Channel Management**: Open and close channels with a few clicks.
*   **Liquidity Tracking**: Monitor inbound and outbound capacity in real-time.
*   **Instant Payments**: Generate and pay Lightning invoices with immediate settlement.

### Persistent Background Service
Lokinode is designed to run in the background to maintain node uptime:
*   **System Tray Integration**: Access common actions and status directly from your OS tray.
*   **Window Management**: Hiding the main window does not stop the node; the process remains active to ensure network connectivity.
*   **Auto-Restart**: Built-in logic to maintain node stability and recover from unexpected interruptions.

### Flexible Node Management
Unlike terminal-based tools, Lokinode allows users to manage multiple node configurations:
*   **Custom Data Locations**: Choose any directory for storing your blockchain data and channel databases.
*   **Real-time Monitoring**: The dashboard provides live updates on sync status, block height, and peer connections.
*   **Security Controls**: Change passwords and manage your recovery phrase within a secure graphic environment.

---

## Managing with Lokihub

While **Lokinode** provides a built-in interface for basic operations, many node operators use **Lokihub** for advanced liquidity management and automated node health dashboards.

### Quick Connection
To connect Lokihub to your Lokinode instance:
1.  **Launch Lokinode**: Ensure your node is running and synchronized.
2.  **Open Lokihub**: Use the web interface or local installation.
3.  **Automated Sync**: Lokihub can automatically detect and retrieve the gRPC credentials from your local node directory.

[View the Lokihub Setup Guide →](/lokihub/setup)

---

## Data Locations

By default, Lokinode stores node data, logs, and channel databases in the standard FLND directory. However, you can specify a custom data location during the initialization process or via the application settings.

| OS | Default Path |
| :--- | :--- |
| **Linux** | `~/.flnd/` |
| **macOS** | `~/Library/Application Support/Flnd/` |
| **Windows** | `%APPDATA%\Flnd\` |

## CRITICAL: Lightning Backups

A 24-word seed phrase **does not recover Lightning channel funds**. It only recovers your on-chain balance.

1.  **Static Channel Backups (SCB)**: Your node generates a `channel.backup` file. This file is required to recover funds from open channels in the event of data loss.
2.  **Requirement**: To fully restore a node, you must have **both** the 24-word seed and the most recent `channel.backup` file.

:::danger[DATA LOSS RISK]
Always ensure you have a backup of your `channel.backup` file. Without it, funds in Lightning channels cannot be recovered if local data is lost.
:::
